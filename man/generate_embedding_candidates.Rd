% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_embedding_candidates.R
\name{generate_embedding_candidates}
\alias{generate_embedding_candidates}
\title{GERIT-Kandidaten per OpenAI-Embeddings erzeugen}
\usage{
generate_embedding_candidates(
  df_scraped,
  df_gerit,
  top_k = 5,
  embedding_model = "text-embedding-3-large",
  batch_size = 100,
  gerit_embedding_cache_file = file.path("data", "cache", paste0("gerit_embeddings_",
    gsub("[^A-Za-z0-9_-]+", "_", embedding_model), ".rds"))
)
}
\arguments{
\item{df_scraped}{Ausgabe von \code{\link[=extract_scraped_organisations]{extract_scraped_organisations()}} nachdem
deterministische Matches bereits angewendet wurden.}

\item{df_gerit}{Ausgabe von \code{\link[=prepare_gerit_data]{prepare_gerit_data()}}.}

\item{top_k}{Maximale Anzahl zurueckgegebener Kandidaten pro Organisation.}

\item{embedding_model}{OpenAI-Embedding-Modellkennung.}

\item{batch_size}{Anzahl der Texte pro Embedding-API-Anfrage.}

\item{gerit_embedding_cache_file}{Pfad zu einer \code{.rds}-Cache-Datei fuer
GERIT-Embeddings. Bereits gespeicherte Embeddings werden wiederverwendet;
fehlende Eintraege werden neu von der OpenAI-API abgerufen. Mit \code{NULL}
wird kein Cache verwendet.}
}
\value{
Ein Tibble gerankter GERIT-Kandidaten mit den Spalten
\code{scraped_ID}, \code{organisation}, \code{cleaned}, \code{gerit_ID}, \code{Einrichtung},
\code{score}, \code{candidate_rank} und \code{candidate_source}. Gibt ein leeres
Tibble zurueck, wenn alle Organisationen bereits gematcht sind.
}
\description{
Bettet alle noch nicht gematchten gescrapten Organisationen und alle
eindeutig benannten GERIT-Eintraege mit dem angegebenen OpenAI-Modell
ein, berechnet Kosinusaehnlichkeiten und gibt die top-\code{k} GERIT-
Kandidaten pro Organisation zurueck.
Erfordert die Umgebungsvariable \code{OPENAI_API_KEY}.
}
\details{
Der Rueckgabewert \code{score} ist die Kosinusaehnlichkeit zwischen dem
Embedding der gescrapten Organisation und dem Embedding der GERIT-
Einrichtung. Die Embeddings kommen aus der OpenAI-Embedding-API mit dem
in \code{embedding_model} gesetzten Modell.

Wichtige Interpretation von \code{score}:
\itemize{
\item Hoher Wert bedeutet: hohe semantische Aehnlichkeit der Texte.
\item Niedriger Wert bedeutet: geringe semantische Aehnlichkeit.
\item Der Wert ist eine Ranking-Groesse fuer die Kandidatenauswahl und
keine kalibrierte Wahrscheinlichkeit fuer einen korrekten Match.
\item Ein guter \code{score} allein reicht nicht als finale Match-Entscheidung;
die finale Entscheidung trifft im naechsten Schritt das LLM.
}
}
