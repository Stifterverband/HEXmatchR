% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_review_decisions.R
\name{apply_review_decisions}
\alias{apply_review_decisions}
\title{Review-Ergebnisse in die Match-Tabelle zurückschreiben}
\usage{
apply_review_decisions(organisation_matches, review_decisions, df_gerit)
}
\arguments{
\item{organisation_matches}{Eine Tabelle mit Organisations-Matches, zum
Beispiel \code{llm_result$scraped} aus \code{\link[=match_scraped_organisations]{match_scraped_organisations()}}.
Sie muss eine Spalte \code{scraped_ID} enthalten.}

\item{review_decisions}{Eine Tabelle mit den manuellen Review-Entscheidungen,
zum Beispiel \code{run_review_app(...)$decisions}. Erwartet werden mindestens
die Spalten \code{scraped_ID} und \code{review_decision}. Je nach Entscheidung werden
außerdem \code{selected_gerit_id} und \code{review_comment} verwendet.}

\item{df_gerit}{Die vorbereiteten GERIT-Daten aus \code{\link[=prepare_gerit_data]{prepare_gerit_data()}}.
Diese werden nur gebraucht, wenn im Review ein alternativer Kandidat
ausgewählt wurde.}
}
\value{
\code{organisation_matches} mit aktualisierten Match-Spalten für alle
bearbeiteten Review-Fälle.
}
\description{
Diese Funktion übernimmt die Entscheidungen aus dem manuellen Review und
aktualisiert damit die passenden Zeilen in \code{organisation_matches}.
}
\details{
Für jede \code{scraped_ID} wird die gewählte Entscheidung angewendet:
\itemize{
\item \code{"accept_model_match"}: den bisherigen Match bestätigen
\item \code{"select_other_candidate"}: einen anderen GERIT-Kandidaten übernehmen
\item \code{"mark_no_match"}: festhalten, dass es keinen passenden GERIT-Eintrag gibt
}

Entscheidungen mit \code{NA} oder \code{"skip"} werden ignoriert. Alle anderen Zeilen
in \code{organisation_matches} bleiben unverändert.
}
