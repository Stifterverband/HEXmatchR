% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_review_app.R
\name{run_review_app}
\alias{run_review_app}
\title{Shiny-Review-App starten}
\usage{
run_review_app(review_cases, df_gerit, reviewed_by = current_username())
}
\arguments{
\item{review_cases}{Ausgabe von \code{\link[=prepare_review_cases]{prepare_review_cases()}}.}

\item{df_gerit}{Vorbereitete GERIT-Daten aus \code{\link[=prepare_gerit_data]{prepare_gerit_data()}}.}

\item{reviewed_by}{Optionaler Reviewer-Name, der in der Spalte
\code{reviewed_by} der zurückgegebenen Entscheidungstabelle gespeichert
wird.}
}
\value{
Eine Liste mit dem Element \code{decisions}: ein Tibble mit je einer
Zeile pro reviewtem Fall und den Spalten \code{scraped_ID}, \code{organisation},
\code{review_decision}, \code{selected_gerit_id}, \code{review_comment},
\code{reviewed_at} und \code{reviewed_by}.
}
\description{
Startet eine interaktive Shiny-Anwendung, die jeden Review-Fall einzeln
präsentiert. Der Reviewer kann einen GERIT-Kandidaten wählen, den Fall
als kein Match markieren oder überspringen. Die App schließt sich, wenn
der Reviewer auf "Fertig"
klickt, und gibt die ausgefüllte Entscheidungstabelle zurück.
Benötigt die Pakete \code{shiny} und \code{DT}.
}
