% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_scraped_organisations.R
\name{match_scraped_organisations}
\alias{match_scraped_organisations}
\title{Gescrapte Organisationen mit GERIT-Einrichtungen matchen}
\usage{
match_scraped_organisations(
  df_scraped,
  df_gerit,
  organisation_col = "organisation",
  year_col = "jahr",
  semester_col = "semester",
  model = "gpt-4.1-mini",
  top_k = 5,
  embedding_model = "text-embedding-3-large",
  embedding_batch_size = 100,
  review_confidence = 0.65
)
}
\arguments{
\item{df_scraped}{Gescrapter Kursdaten-Data-Frame.}

\item{df_gerit}{Vorbereitete GERIT-Daten aus \code{\link[=prepare_gerit_data]{prepare_gerit_data()}}.}

\item{organisation_col}{Spalte in \code{df_scraped} mit den Organisationsnamen.}

\item{year_col}{Spalte in \code{df_scraped} mit der Jahresangabe.}

\item{semester_col}{Spalte in \code{df_scraped} mit der Semesterangabe.}

\item{model}{OpenAI-Modell für \code{ellmer::chat_openai()}.}

\item{top_k}{Anzahl der GERIT-Kandidaten, die pro Organisation an das
LLM übergeben werden.}

\item{embedding_model}{OpenAI-Embedding-Modell für den Kandidatenabruf.}

\item{embedding_batch_size}{Batch-Größe für Embedding-Anfragen.}

\item{review_confidence}{Unterer Schwellenwert, unterhalb dessen ein
Fall zur Review weitergeleitet wird.}
}
\value{
Eine Liste mit den Elementen:
\describe{
\item{\code{organisation_matches}}{Organisationsebene-Tibble mit allen
befüllten Match-Spalten.}
\item{\code{candidates}}{Kandidaten-Tibble aus dem Embedding-Retrieval.}
\item{\code{llm_decisions}}{Rohe LLM-Entscheidungen als Tibble.}
\item{\code{df_scraped_matched}}{Vollständiger gescrapter Data Frame mit
zurückgefügten Match-Spalten.}
}
}
\description{
Orchestriert die vollständige Matching-Pipeline: extrahiert eindeutige
Werte aus der gescrapten Spalte \code{organisation}, matched sie gegen
\code{Einrichtung} aus den vorbereiteten GERIT-Daten, generiert
Embedding-Kandidaten, führt LLM-basiertes Matching durch und fügt die
Ergebnisse in die ursprünglichen gescrapten Daten ein.
}
\details{
Der in den Kandidaten enthaltene \code{score} stammt aus dem Embedding-
Retrieval und dient nur dazu, Kandidaten nach semantischer Aehnlichkeit
zu sortieren. Die finale Match-Entscheidung trifft das LLM im Schritt
\code{\link[=match_organisations_with_llm]{match_organisations_with_llm()}} auf Basis der Kandidatenliste,
Bezeichnungen und des Prompts.

Unsicherheit wird in der Praxis ueber \code{review_confidence} gesteuert:
Entscheidungen unterhalb des Schwellenwerts (oder ohne Konfidenz) werden
als Review-Faelle markiert.
}
