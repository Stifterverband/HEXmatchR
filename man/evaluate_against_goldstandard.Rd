% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_against_goldstandard.R
\name{evaluate_against_goldstandard}
\alias{evaluate_against_goldstandard}
\title{Matching-Ergebnisse mit einem Goldstandard vergleichen}
\usage{
evaluate_against_goldstandard(
  organisation_matches,
  gold_data,
  organisation_col = "organisation"
)
}
\arguments{
\item{organisation_matches}{Eine Match-Tabelle auf Ebene der gescrapten
\code{organisation}, zum Beispiel die Ausgabe von
\code{\link[=match_scraped_organisations]{match_scraped_organisations()}}. Erwartet werden die aus GERIT
übernommenen Spalten \code{gerit_organisation}, \code{Faechergruppen} und
\code{LUF_IDs}.}

\item{gold_data}{Ein Data Frame oder ein Pfad zu einer \code{.rds}-Datei mit den
manuellen Referenzdaten. Erwartet werden eine Organisationsspalte,
\code{matchingart}, \code{luf_code} und \code{faechergruppe}.
Groß-/Kleinschreibung der Spaltennamen ist egal.}

\item{organisation_col}{Name der Organisationsspalte in \code{gold_data}.}
}
\value{
Eine Liste mit drei Elementen:
\describe{
\item{\code{comparison}}{Vergleichstabelle mit Vorhersage, Goldstandard und
Korrektheitsindikatoren pro Organisation.}
\item{\code{metrics}}{Tibble mit den wichtigsten Gesamtmetriken. Enthält die
Spalten \code{metric} und \code{value}. \code{value} ist jeweils wie folgt berechnet:
\code{n_organisations = nrow(comparison)};
\code{n_with_gold_luf = sum(has_gold_luf)};
\code{match_rate = mean(matched == "yes")};
\code{review_rate = mean(needs_review)};
\code{luf_accuracy = mean(luf_correct[has_gold_luf])}.}
\item{\code{by_gold_matchingart}}{Zusammenfassung der Metriken getrennt nach
\code{matchingart} aus dem Goldstandard. Für jede Gruppe werden \code{n},
\code{match_rate}, \code{review_rate} und \code{luf_accuracy} mit derselben
Logik wie oben innerhalb der jeweiligen Teilmenge berechnet.}
}
}
\description{
Diese Funktion prüft, ob die im Matching vorhergesagten \code{LUF_IDs}
aus den gematchten GERIT-Einrichtungen mit dem Goldstandard aus
\code{gold_data$luf_code} übereinstimmen.
}
\details{
Dafür werden die gematchten GERIT-Einrichtungen über \code{organisation}
mit dem Goldstandard verknüpft und drei Ergebnisse zurückgegeben:
\itemize{
\item eine Vergleichstabelle auf Zeilenebene
\item einige Gesamtmetriken
\item eine Auswertung nach \code{matchingart} aus dem Goldstandard
}

Die zurückgegebenen Gesamtmetriken sind:
\itemize{
\item \code{n_organisations}: Anzahl der Organisationen in \code{organisation_matches}
\item \code{n_with_gold_luf}: Anzahl der Organisationen, für die im Goldstandard ein
\code{luf_code} vorliegt
\item \code{match_rate}: Anteil der Organisationen mit \code{matched == "yes"}
\item \code{review_rate}: Anteil der Organisationen mit \code{needs_review == TRUE}
\item \code{luf_accuracy}: Anteil der Organisationen, bei denen die
vorhergesagten \code{LUF_IDs} exakt mit dem Goldstandard-Wert in
\code{luf_code} übereinstimmen. Die Reihenfolge der Codes wird dabei
ignoriert, sodass z. B. \code{231|771} und \code{771|231} als identisch gelten.
Diese Metrik wird nur für
Organisationen berechnet, für die im Goldstandard überhaupt ein
Vergleichswert vorliegt.
}
}
