% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_organisations_with_llm.R
\name{match_organisations_with_llm}
\alias{match_organisations_with_llm}
\title{Fachgebiete per LLM (OpenAI via ellmer) matchen}
\usage{
match_organisations_with_llm(
  df_scraped,
  candidate_tbl,
  df_gerit,
  model = "gpt-4.1-mini",
  review_confidence = 0.65,
  temperature = 0
)
}
\arguments{
\item{df_scraped}{Ausgabe von \code{\link[=extract_scraped_organisations]{extract_scraped_organisations()}} nachdem
deterministische Matches angewendet wurden.}

\item{candidate_tbl}{Ausgabe von \code{\link[=generate_embedding_candidates]{generate_embedding_candidates()}}.}

\item{df_gerit}{Ausgabe von \code{\link[=prepare_gerit_data]{prepare_gerit_data()}}.}

\item{model}{OpenAI-Modellname fuer \code{ellmer::chat_openai()}.}

\item{review_confidence}{Numerischer Schwellenwert; LLM-Entscheidungen
darunter werden fuer Review markiert.}

\item{temperature}{Numerischer Wert zwischen 0 und 1 fuer die
Zufaelligkeit des LLM. Standard: 0 fuer deterministische Ausgaben.}
}
\value{
Eine Liste mit den Elementen:
\describe{
\item{\code{scraped}}{Aktualisiertes Fachgebiets-Tibble mit
eingetragenen LLM-Matches.}
\item{\code{decisions}}{Tibble der rohen LLM-Entscheidungen, eine Zeile
pro verarbeitetem Fachgebiet.}
}
}
\description{
Erstellt fuer jedes noch nicht gematchte gescrapte Fachgebiet einen
strukturierten Prompt aus den top-k Kandidaten und befragt das
LLM nach dem besten Treffer oder keinem Match. Review-Faelle werden nur
fuer wirklich unsichere Entscheidungen markiert: bei fehlender Konfidenz
oder unter \code{review_confidence}.
}
\details{
Wie OpenAI hier die Match-Entscheidung trifft:
\itemize{
\item Das Modell sieht pro Organisation nur die top-k Kandidaten aus dem
Embedding-Retrieval (inklusive \code{embedding_score}).
\item Es darf ausschliesslich zwischen zwei Entscheidungen waehlen:
\code{select_candidate} oder \code{no_match}.
\item Bei \code{select_candidate} muss \code{selected_candidate_id} exakt eine
erlaubte \code{gerit_ID} aus der Kandidatenliste sein.
\item Die Antwort wird ueber ein striktes JSON-Schema validiert, damit keine
anderen Entscheidungswerte oder zusaetzlichen Felder auftreten.
}

Wie Unsicherheit behandelt wird:
\itemize{
\item Das Modell gibt eine \code{confidence} im Bereich \link{0, 1} zur eigenen
Entscheidung an.
\item Ein Fall wird als unsicher fuer Review markiert, wenn
\code{confidence < review_confidence} oder \code{confidence} fehlt.
\item Das vom Modell gelieferte Feld \code{needs_review} wird ebenfalls
beruecksichtigt; die Pipeline setzt Review aber in jedem Fall, sobald
die Konfidenz unter dem Schwellenwert liegt.
\item \code{review_confidence} ist damit der praktische Punkt, ab dem die
automatische Entscheidung als nicht ausreichend sicher gilt.
}
}
